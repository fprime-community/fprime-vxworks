The purpose of this VxWorks.cmake platform file is to serve as a starting point. Projects are expected to create their own `VxWorks-<platform>.cmake` platform file in their project's repository. Projects can choose their preferred F Prime implementation for specific OS features, and they can include their own `types` directory. Most projects can include the `types` directory located in this directory.
